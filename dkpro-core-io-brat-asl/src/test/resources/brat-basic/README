This directory contains brat files where the .ann files only
contain UIMA types that are defined in the BratReader's 
default types.

Moreover, the types are provided using the short names
(ex: Token) as opposed to the fully qualified class name.